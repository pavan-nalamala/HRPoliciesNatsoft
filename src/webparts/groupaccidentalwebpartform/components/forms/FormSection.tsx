import * as React from 'react';

export interface IFormSectionProps {
  title: string;
}

const FormSection = ({ title }: IFormSectionProps): JSX.Element => {
  return (
    <div>
      {/* <h4>{title}</h4>s */}
    </div>
  );
};

export default FormSection;
